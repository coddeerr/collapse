# QC - homestead_base

Status: accepted

Skill: generate2dmap

Result:

- Foundation-only map base generated through Image2.
- Contains ground, dirt paths, build pads, farm pads, and polluted boundary.
- Does not contain runtime buildings, trees, characters, monsters, UI, labels,
  or props that should be independently controlled.

Runtime copy:

- `art/maps/homestead_v1/base.png`

